name: Deploy to Firebase

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      # Install dependencies in the root project
      - name: Install dependencies for Next.js
        run: npm ci

      # Build the Next.js project
      - name: Build Next.js
        run: npm run build
        env:
          # Add any environment variables needed for the build
          NEXT_PUBLIC_FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: ${{ secrets.FIREBASE_AUTH_DOMAIN }}
          NEXT_PUBLIC_FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID || 'geobit-959c9' }}
          NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: ${{ secrets.FIREBASE_STORAGE_BUCKET }}
          NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.FIREBASE_MESSAGING_SENDER_ID }}
          NEXT_PUBLIC_FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
          NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID: ${{ secrets.FIREBASE_MEASUREMENT_ID }}

      # Install dependencies in the functions directory
      - name: Install dependencies for Firebase Functions
        run: cd functions && npm ci

      # Set up the Firebase service account
      - name: Set up Firebase service account
        run: |
          echo '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}' > /tmp/firebase-service-account.json
          export GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-service-account.json

      # Update .firebaserc with explicit hardcoded project ID
      - name: Create .firebaserc with explicit project ID
        run: |
          echo '{
            "projects": {
              "default": "geobit-959c9"
            }
          }' > .firebaserc
          cat .firebaserc

      # Using a simpler approach with explicit Firebase CLI commands
      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      # Deploy directly with explicit project ID
      - name: Deploy to Firebase
        run: |
          if [ -n "${{ secrets.FIREBASE_SERVICE_ACCOUNT }}" ]; then
            # Deploy with service account
            export GOOGLE_APPLICATION_CREDENTIALS=/tmp/firebase-service-account.json
            firebase deploy --project=geobit-959c9
          else
            # Deploy with non-interactive mode
            firebase deploy --project=geobit-959c9 --non-interactive
          fi
